namespace VanguardTracker.Api;

/// <summary>Single source of truth for the API version — keep in sync with the
/// &lt;Version&gt; in VanguardTracker.Api.csproj and frontend/package.json.</summary>
public static class AppInfo
{
    public const string Version = "0.1.0";
}
